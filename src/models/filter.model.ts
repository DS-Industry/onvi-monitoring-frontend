export interface FilterDepositPos {
    dateStart: string;
    dateEnd: string;
    posId?: number;
}

export interface FilterDepositDevice {
    dateStart: string;
    dateEnd: string;
    deviceId?: number;
}